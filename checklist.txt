X input form: fName, lName, id, title, salary, & submit button
X capture click on submit button
upon submit click: 
    X create a new employee object with user input
    X clear input fields
    push that employee into an array
    calculate monthly salary
    append employees to DOM
    append total monthly to DOM
    show if exceeds max monthly salary of 20k 
delete employee row on button click